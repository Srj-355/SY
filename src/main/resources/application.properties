spring.application.name=sy
# Server port
server.port=8080

# Thymeleaf configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false 

# PostgreSQL configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/sy
spring.datasource.username=postgres
spring.datasource.password=superus
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=saroj80977@gmail.com
spring.mail.password=ydrx ssxz uvwf qerf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application base URL
app.base-url=http://localhost:8080
app.payment-timeout=5000

# eSewa Configuration
esewa.merchant.id=EPAYTEST
esewa.initiate-url=https://rc-epay.esewa.com.np/api/epay/main/v2/form
esewa.secret-key=8gBm/:&EnhH.1/q
esewa.test-mode=true

# Session configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.name=SY_SESSION
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=lax
server.servlet.session.tracking-modes=COOKIE

# Static resources cache
spring.web.resources.cache.cachecontrol.max-age=365d
spring.web.resources.cache.cachecontrol.no-cache=false

# CSRF protection
spring.security.csrf.enabled=true